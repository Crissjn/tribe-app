import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="card-carousel"
// export default class extends Controller {
//   static targets = ['carousel-track'];

//   initialize() {
//     this.scrollAmount = this.element.offsetWidth / 4; // Scroll by the width of one card
//   }
